/* .Implemente uma função que receba como parâmetros um vetor de números inteiros v e seu número de elementos n, 
e verifique se os elementos do vetor correspondem aos termos de uma PA (progressão aritmética)
, isto é, se existe um número inteiro k tal que v[i] seja igual a v [0] + i*k. 
Caso os elementos do vetor correspondam aos termos de uma PA, a função deve retornar o valor calculado para k.
 Por exemplo, se for passado para essa função o vetor {2,10,18,26,34} , ela deve retornar o valor 8.
  Caso o vetor não corresponda a uma PA, a função deve retornar 0. 
  Assuma que o vetor sempre terá pelo menos três elementos. Tal função deve ter o seguinte protótipo:
*/

#include <stdio.h>

int testa_PA (int n, int * v){

}

int main(){
  
  return 0;
}
